*Симметричный подход шифрования изначально несет в себе очевидную проблему, называемую **распределением ключей*** (key distribution)

***Проблема заключается:*** отправитель и получатель хотят обмениваться секретными сообщениями, но в их распоряжении имеется незащищенный канал

*Единственный по-настоящему надежным решением этой проблемы является **передача ключа при личной встрече***  

*Другим, менее надежным способом распределения ключей является **использование курьеров или других вариантов защищенной доставки ключей***

![[Pasted image 20251209195840.png]]
*Плохая масштабируемость симметричного алгоритма шифрования – количество ключей, требуемых для обмена секретными сообщениями, пропорционально квадрату участников обмена*

***Односторонняя функция (one-way function)*** - это функция y=F(x), которая легко вычисляется для любого входного значения x, но обратная задача - определение x по заданному значению функции y - решается очень трудно 

![[Pasted image 20251209200142.png]]

***Примеры односторонних функций:*** 
- Простейшая функция двух аргументов F(p,q)=pq, представляющая собой произведение двух простых чисел p и q, вычисляется сравнительно просто, даже если числа p и q очень большие. Но чрезвычайно сложно решить обратную задачу (называемую факторизацией): по значению произведения подобрать исходные два простых числа.
- Функция взятия модуля - Y(x) = x mod n, где операция x(mod n) («x по модулю n») - дает в результате остаток от деления x на n. Эта функция является односторонней, так как зная остаток от деления x на n, невозможно однозначно определить значение аргумента x.
- Функция Y(x)=Dx (mod P) при некоторых ограничениях на значения параметров D и P является односторонней, то есть зная значение Y, а также значения параметров D и P, нельзя без экстраординарных вычислительных усилий найти значение аргумента x.

![[Pasted image 20251209201424.png]]

***Для того, чтобы усложнить решение обратной задачи, то есть восстановлению закрытого ключа Алисы или Боба по открытому, на значения параметров алгоритма накладываются некоторые ограничения, в том числе следующие:***  
- Все параметры D, P, A, B – целые положительные числа;  
- _A_ и _B_ должны быть большими числами порядка 10100;  
- P должно быть большим простым числом порядка 10300, причем желательно, чтобы (P-1)/2 также было простым числом;  
- _D_ не обязательно должно быть большим, обычно оно выбирается меньше десяти, D < P
